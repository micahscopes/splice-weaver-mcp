# AST-Grep CLI Deep Dive Exploration

## Goal
Explore ast-grep CLI features that may not be exposed by the existing MCP server to understand what LLMs are missing.

## Exploration Log

### Step 1: Full CLI Documentation Review